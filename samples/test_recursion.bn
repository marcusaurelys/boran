const fibonacci : fn = (x : int) : int => {
    if x <= 1 {
        return x;
    }
    return fibonacci(x - 1) + fibonacci(x-2);
};

const main : fn = () : int => {
    let val : int = fibonacci(5);
    print(val);
};

main();